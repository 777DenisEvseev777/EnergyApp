import React from 'react';
import style from '../moduleCSS/Register.module.css';

const Register= () => {
    return (
        <div className={style.Register}>
            register
        </div>

    );
}

export default Register;